# MCP panel testing

This document provides reference patterns for testing MCP/A2A UI panels.

- Validate JSON-RPC requests sent by the UI.
- Validate UI state transitions after responses.
- Use role-based selectors.
